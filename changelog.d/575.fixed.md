BGP sessions that go down are reported again. On routers that send Zino an SNMP trap when a session drops, the drop was only written to the log and never turned into an event, so the outage went unreported
