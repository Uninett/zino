name: Linting
on:
  push:
    branches: "master"
  pull_request:

jobs:
  ruff:
    runs-on: ubuntu-latest
    name: Lint Python
    steps:
    - uses: actions/checkout@v7
      with:
        persist-credentials: false
    - uses: astral-sh/ruff-action@v3
      with:
        version: "0.14.13"
